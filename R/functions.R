add_one <- function(x) {
  x + 1
}

# Exemple de modifications de Bob pour le scénario 1 : dé-commenter les lignes 6 à 9
# add_two <- function(x) {
#   x <- add_one(x)
#   return(add_one(x))
# }

# ** A dé-commenter et commit avant de démarrer le scénario 2 **
# add_n <- function(x, n) {
#
#   for (i in 1:n) {
#    x <- add_one(x)
#   }
#
#   return(x)
# }
